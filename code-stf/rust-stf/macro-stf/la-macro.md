eat codes,consume into AST, generate new AST

`cargo expand` would turn post-AST back to code?

whichever kinda macro eats its input

`macro_rule!` eats everything follows until a `;`?

a compile time process
