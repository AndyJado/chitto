la: bool {
  File is not a ast node,
  comment not in AST,
  does git recogonize AST?
  #[lang = "rust"]
  is bool a enum? no,
  
  
}

```rust
pub struct Da {
  // List
  #[builder(each = "aa")]
  #[builder(Copy)]
  // Path
  #[test]
  // NameValue
  #[path = "aa"]
  aas: Option<std::String>
}
```