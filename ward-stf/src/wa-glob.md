`glob` is string matching in terminal

`wildcard` like `*`,`?`

`regex` lives in code

string.*`exacchar matchinghese words.*/glob is regex in termina`wildcard  like `*a,`?n

`/.*exactly these words.*/`