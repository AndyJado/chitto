Can I somehow persist some macro scripts?

a use case example:

Me writing markdown and demand a rust-code snippt

I'd like to have a macro who would produce:

```rust
todo!()

```

I have no trouble writing this macro

but man how do I persist?